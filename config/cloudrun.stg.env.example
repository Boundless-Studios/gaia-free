# Cloud Run staging-specific overrides
# Copy to cloudrun.stg.env and update values
# Or run `make setup-private` if you have access to gaia-private

ENV=stg
ENVIRONMENT=staging
ENVIRONMENT_NAME=stg
PORT=8080

# CORS/WS allowlist for staging
WS_ALLOWED_ORIGINS=https://stg.your-domain.com
CORS_ALLOWED_ORIGINS=https://stg.your-domain.com

# GCS buckets for staging
CAMPAIGN_STORAGE_BUCKET=your-campaigns-stg
CLIENT_AUDIO_BUCKET=your-media-stg
IMAGE_STORAGE_BUCKET=your-media-stg

# Database
REQUIRE_DATABASE_ON_STARTUP=true

# Audio configuration
AUTO_TTS_ENABLED=false

# Secret Manager entries
SECRETS_ON_STARTUP=DB_PASSWORD=your-db-password-secret@latest
