# Cloud Run production-specific overrides
# Copy to cloudrun.prod.env and update values
# Or run `make setup-private` if you have access to gaia-private

ENV=prod
ENVIRONMENT=production
ENVIRONMENT_NAME=prod
PORT=8080

# CORS/WS allowlist for production
WS_ALLOWED_ORIGINS=https://your-domain.com
CORS_ALLOWED_ORIGINS=https://your-domain.com

# GCS buckets for production
CAMPAIGN_STORAGE_BUCKET=your-campaigns-prod
CLIENT_AUDIO_BUCKET=your-media-prod
IMAGE_STORAGE_BUCKET=your-media-prod

# Require database connection on startup in production
REQUIRE_DATABASE_ON_STARTUP=true

# Audio configuration
AUTO_TTS_ENABLED=false

# Secret Manager entries (format: LOCAL_VAR=secret-name@version)
SECRETS_ON_STARTUP=DB_PASSWORD=your-db-password-secret@latest
